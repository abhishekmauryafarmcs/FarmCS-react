const { createClient } = require('@supabase/supabase-js');

const supabaseUrl = 'https://tjgypejzectqcxznueqi.supabase.co';
const supabaseKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InRqZ3lwZWp6ZWN0cWN4em51ZXFpIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjQ0MTk2MjYsImV4cCI6MjA3OTk5NTYyNn0.Lfzsih7rlemxuiwB91qsB1_6kI7Y6gPzvdgVr8I_C_k';

// Note: You need to use the service_role key to execute SQL
// For now, I'll provide the SQL that you need to run manually in Supabase Dashboard

const createUsersTableSQL = `
-- Create users table
CREATE TABLE IF NOT EXISTS users (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    first_name TEXT NOT NULL,
    last_name TEXT NOT NULL,
    mobile TEXT NOT NULL UNIQUE,
    state TEXT NOT NULL,
    district TEXT NOT NULL,
    password_hash TEXT NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Enable RLS
ALTER TABLE users ENABLE ROW LEVEL SECURITY;

-- Create policy for users to only see their own data
CREATE POLICY "Users can only view their own data" ON users
    FOR SELECT USING (auth.uid()::text = mobile);

-- Create policy for users to insert their own data
CREATE POLICY "Users can insert their own data" ON users
    FOR INSERT WITH CHECK (auth.uid()::text = mobile);

-- Create policy for users to update their own data
CREATE POLICY "Users can update their own data" ON users
    FOR UPDATE USING (auth.uid()::text = mobile);

-- Create index for faster lookups
CREATE INDEX IF NOT EXISTS idx_users_mobile ON users(mobile);

-- Create trigger to update updated_at timestamp
CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = NOW();
    RETURN NEW;
END;
$$ language 'plpgsql';

CREATE TRIGGER update_users_updated_at BEFORE UPDATE
    ON users FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
`;

console.log('Please run the following SQL in your Supabase Dashboard SQL Editor:');
console.log('================================================================');
console.log(createUsersTableSQL);
console.log('================================================================');
console.log('\nSteps:');
console.log('1. Go to https://supabase.com/dashboard/project/tjgypejzectqcxznueqi');
console.log('2. Click on "SQL Editor" in the sidebar');
console.log('3. Click "New query"');
console.log('4. Copy and paste the SQL above');
console.log('5. Click "Run" to execute the migration');
console.log('\nNote: This will create the users table with proper RLS policies for your authentication system.');
